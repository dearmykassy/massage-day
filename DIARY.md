# 마사지데이 활동 일지

> 새 활동과 검증 결과를 최신순으로 기록한다. 비밀값·로그인 정보는 기록하지 않는다.

## 2026-08-19 KST — 홈 상단 문구 교체

- 홈 히어로 H1을 `주소와 이용 항목을 먼저 확인하는 마사지데이`에서 `하루의 마지막은 마사지데이`로 교체했다. 브랜드의 줄바꿈 방지 span은 유지하고, 나머지 고객 콘텐츠·이미지·URL·메타데이터는 변경하지 않았다.
- 홈 전용 sitemap revision을 분리해 콘텐츠 변경 커밋 `f7daede7`의 실제 Git author 시각 `2026-08-19T05:46:22+09:00`을 `/`에만 적용했다. `/areas/`와 광역 41개, 고정·세부 1,250개, 블로그 글의 기존 revision은 그대로 유지한다.
- 집중 테스트 12개와 전체 Vitest 50개, 7개 정본 copy audit, TypeScript, 전체 ESLint, 연속 production build 2회와 built-output 감사를 통과했다. 두 빌드의 sitemap SHA-256은 `1b5caa876907f83729fd22f5f159eedbb7e6d18de60ca552ce5647b0a5a7690a`로 같고, 320·390·768·1440px 브라우저에서 새 H1 표시·히어로 이미지 로드·가로 overflow 0을 확인했다.

## 2026-08-19 KST — HTTPS 네이버 소유확인 정본화

- 네이버에 등록한 운영 URL은 canonical과 같은 `https://msgday.kr`이며, HTTPS 과제에서 발급된 메타 토큰 `e4336b3a46780c9dc349116dc3c43c84c4cae1eb`을 루트 `<head>`의 단일 소유확인 값으로 고정했다.
- `http://msgday.kr` 과제에서 별도로 발급됐던 `96effac4a012f26f5aad2616d58c159cdcfc2a87`은 HTTPS 소유확인에 사용할 수 없으므로 활성 HTML에서 제거하고 회귀 감사로 재유입을 차단한다. HTTP 요청은 기존처럼 HTTPS canonical로 이동시킨다.
- 메타 방식과 별도로 같은 HTTPS 과제의 `naverf76d996ba16d8e0fc251624cf0ebcd0e.html` 확인 파일을 루트에 추가해 네이버 권장 HTML 파일 방식도 지원한다. 빌드와 운영 배포에서 메타 토큰 1개, 확인 파일의 정확한 본문과 HTTP 200을 함께 검증한다.
- 집중 테스트 10개와 전체 Vitest 49개, 7개 정본 copy audit, TypeScript, 전체 ESLint, production build 1,304개 route와 built-output 감사를 통과했다. 빌드된 루트 `<head>`에는 HTTPS 토큰이 정확히 1개이고 HTTP 과제 토큰은 0개이며, HTTPS 확인 파일의 본문은 네이버 표준 문자열과 일치한다.

## 2026-08-19 KST — 네이버 소유확인 토큰 갱신(폐기된 HTTP 과제)

- 네이버의 `http://msgday.kr` 과제에서 발급된 마사지데이 소유확인 토큰 `96effac4a012f26f5aad2616d58c159cdcfc2a87`로 루트 메타태그를 한때 갱신했다.
- Metadata API 테스트와 정적 빌드 감사가 새 토큰을 정확히 1개 요구하도록 함께 변경했다. 이전 토큰과 이전 HTML 확인 파일 후보는 활성 배포에서 제거한다.
- 이후 canonical과 동일한 HTTPS 등록이 정본임을 확인해 위 HTTPS 소유확인 기록으로 대체했다.

## 2026-08-19 KST — 운영 링크 prefetch 차단·sitemap revision 정교화

- 내부 `next/link` 사용 12곳을 단일 `SiteLink`로 모으고 운영 환경에서는 전달값과 관계없이 `prefetch=false`를 강제했다. 링크는 계속 Next Link의 실제 `<a href>`와 이동·핸들러·ARIA 속성을 유지하며, wrapper 밖 직접 import를 회귀 테스트로 차단한다.
- sitemap 1,299개 URL의 `lastmod`를 실제 Git author 시각 기준으로 분리했다. 홈·전체 지역·광역 41개는 짧은 고객 노출 지역명 변경 커밋, 고정 4개·세부 1,250개는 `msgday.kr` 공개 커밋, 블로그 2개는 각 실제 `modifiedAt`을 사용한다. 기존의 임의 00:00 출시 시각과 `changefreq`·`priority`는 제거했다.
- 전체 Vitest 48개, 7개 정본 copy audit, TypeScript, ESLint, production build 1,304개 route와 built-output 감사를 통과했다. 산출물 sitemap은 `43 + 1,254 + 1 + 1 = 1,299`개의 정확한 revision 그룹, `changefreq`·`priority` 0개이며 홈 HTML은 실제 anchor 70개와 `_rsc` query literal 0개다.

## 2026-08-19 KST — 광역·시 고객 노출 지역명 축약

- 홈과 전체 지역의 시작 권역 카드는 `서울특별시`, `인천광역시`, `천안시` 대신 `서울`, `인천`, `천안`처럼 짧게 표시한다.
- 광역 41개 페이지는 H1·H2, breadcrumb, 도입 문장 82개, 본문 문단 902개, 직계 하위 시 카드 30개까지 같은 축약 계약을 적용했다. 제주 도 단위 heading은 `제주 전역`, 제주시 heading은 `제주`로 구분해 heading 고유성을 유지한다.
- 같은 축약명이 겹치던 제주 도 단위와 제주시의 검색형 메타도 각각 `제주 전역`, `제주`로 나눠 기존 `제주 제주` 중복 표기를 제거했다.
- 세부 1,250개 구·군·읍·면·동·리 페이지, 활성 URL 1,291개, canonical과 정본 행정 데이터는 변경하지 않았다. 전수 회귀 테스트는 광역 고객 노출 표면에서 긴 행정명 0건과 정확한 대상 개수를 검사한다.

## 2026-08-19 KST — msgday.kr 운영 도메인 전환

- 확정된 운영 도메인 `https://msgday.kr`을 canonical, Open Graph, sitemap, RSS, robots, 구조화 데이터의 단일 origin으로 적용했다. 배포 게이트를 공개 상태로 전환하고 모든 페이지를 `index,follow`, robots를 전체 허용으로 변경했다.
- 네이버 소유확인 메타태그는 그대로 유지한다. 마사지데이 전용 GA4 측정 ID는 배포 환경에 연결한 뒤 운영 HTML과 이벤트 전송 계약을 별도로 검증한다.

## 2026-08-19 KST — 네이버 소유확인 메타태그 추가

- 네이버에서 발급된 마사지데이 전용 소유확인 값을 루트 Metadata API의 `naver-site-verification` 항목에 연결했다. 모든 정적 페이지의 `<head>`에서 동일한 태그가 출력되도록 하고, 정확한 값과 빌드 산출물을 회귀 검사한다.
- 이 시점에는 운영 도메인이 미정이어서 preview 차단 계약을 유지했으며, 이후 `msgday.kr` 확정에 따라 위 운영 전환 기록에서 공개 계약으로 변경했다.

## 2026-08-19 KST — 지역 메타 고객 검색형 행정명 적용

- 지역 meta title·meta keywords·meta description의 대상 지역명에서 행정 토큰 끝의 `특별자치도`, `특별자치시`, `특별시`, `광역시`, `도`, `시`만 제거한다. 서울특별시→서울, 인천광역시→인천, 경기도→경기, 제주특별자치도→제주, 수원시→수원, 천안시→천안으로 맞춘다.
- `구·군·읍·면·동·리`는 유지하고, 같은 이름이 여러 주소에 있으면 축약한 상위 지역을 함께 사용한다. H1·본문·breadcrumb·schema의 공식 행정명과 기존 URL·canonical은 변경하지 않는다.
- 활성 1,291개 지역 메타를 전수 검사해 고객 검색형 지역명, 메타 3종 고유성, 공식 수식어와 서비스 키워드의 잘못된 결합이 다시 생기지 않도록 회귀 테스트를 고정한다.

## 2026-08-19 KST — 홈 지역 카드 목적 정리

- 주요 지역 카드 뒤에 Template6의 12칸을 채우기 위해 붙였던 `전체 지역`, `코스·가격`, `이용 방법`, `안내 글` 일반 메뉴 카드 4개를 제거했다. 해당 링크는 상단 내비게이션과 각 전용 섹션에 이미 있어 지역 카드 영역에서 반복할 이유가 없다.
- 홈의 첫 카드 영역은 서울·인천·경기·천안·아산·대전·대구·구미의 실제 지역 카드 8개만 표시한다. 전체 11개 시작 권역 디렉터리는 기존처럼 홈 마지막 섹션에 유지한다.

## 2026-08-18 KST — Template6 플랫폼 최종 검증

- 전체 Vitest 39개, 기존 7개 플랫폼 copy audit, TypeScript 검사, 전체 ESLint, Next.js production build와 built-output audit를 통과했다. 정적 공개 페이지는 1,299개이고 지역 페이지는 1,291개다. sitemap URL 1,299개, RSS 본문 글 2개, 지역 파생 WebP 648개를 산출물에서 다시 확인했다.
- 실제 브라우저에서 320·390·768·1440px를 확인했다. 가로 넘침은 0이고 홈 카드 열은 각각 2·2·3·4열이며, 히어로와 지역 배너가 로드된다. `수원` 검색 결과에서 경기도 수원시 페이지로 직접 이동했고 홈·지역 페이지의 주소 디렉터리가 마지막 콘텐츠 섹션에 유지됨을 확인했다.
- 운영 도메인이 정해지지 않아 `https://preview.massage-day.invalid` canonical, `noindex,nofollow,nocache`, robots 전체 차단을 유지한다. 운영 도메인·전용 GA4 ID·검색엔진 소유확인 값은 임의로 넣지 않았다.
- 공개 파생 이미지와 최소 권한 JSON만 버전 관리 대상으로 두고, 약 400MB의 생성·재사용 마스터와 접촉시트·중간 영수증, `.next`, `out`, `node_modules`는 재생성 가능한 로컬 산출물로 제외한다.

## 2026-08-18 KST — 이미지 루트 승인·최종 릴리스

- 지역 마스터 216장과 에디토리얼 3장을 19개 접촉시트로 전수 검수했다. 성인 한국 여성 1명, 실제 거울·휴대폰, 완전 착의, 일관된 단일 반사, 320×300·390×300 모바일 인셋, 문자·로고·워터마크·침대·욕실·기형 없음 기준에서 219개 ACCEPT, 0개 REJECT다.
- 최종 재사용 72장은 필링홈타이 24장, 건마에반하다 24장, 혼혈마사지 24장이다. 초기 랑테라피 후보 24장은 거울 셀피 개념과 달라 활성 선택에서 제외하고 보존했으며, 필링홈타이 루트 승인·릴리스 완료본으로 byte-exact 대체했다.
- 최초 생성본에서 반려된 논리 자산 27개는 승인 교체본으로 연결했다. 최종 선택 버전은 v2 13개, v3 13개, v4 1개이며 실패 후보와 계보 해시는 삭제하지 않았다.
- 재사용 001~024는 자산별 focal point를 적용해 1,600×900 원본에서 768×600 모바일 파생 후 실제 CSS 320×300·390×300 cover를 확인했다. 24개 모두 얼굴·휴대폰·거울이 유지됐고 focal SHA-256은 `5fb69d5679502bed1018c8d789ffe16912fce3d6827cc5a22c69acf9718f12ae`다.
- 루트 승인 입력 SHA-256 `88f006f677344b3e8fa0ea82484d213eaef20c3cfff744b7d4c125d4da5cb558`과 승인 영수증 SHA-256 `6f6f49894982d8b159b1596c62b5bd06747613d0bf75f59599858f329de663f4`에 216개 지역·3개 에디토리얼 ACCEPT와 경로 배정 권한을 결속했다.
- 최종 상태는 `ROOT_APPROVED_RELEASED`다. 지역 216개 디렉터리에 데스크톱·태블릿·모바일 WebP 648개와 provenance 216개, 홈 지역 카드 11개, 홈·블로그 WebP 3개를 릴리스했다. 재사용 430경로·신규 861경로, 최대 6회, 광역 41개 전부 신규, 부모·자식 충돌 0·형제 충돌 0이다. 릴리스 영수증 SHA-256은 `afe693bc10c6aac19e240175ccce2257d5bd66eff6e2eec11af59544ea7bcbcb`다.

## 2026-08-18 KST — 재사용 001~024 거울 셀피 후보 교체

- 기존 랑테라피 기반 `reused` 원본 24개는 그대로 보존하고, 필링홈타이 Template3의 루트 승인·릴리스 완료 `rng-t3-rgn-001-v1`~`024-v1` desktop WebP를 신규 `regional-masters/reused-v2` 경로에 byte-exact 복사했다. 원본과 복사본 SHA-256은 24/24 일치하며 모두 1600×900 WebP다.
- 필링홈타이 루트 검수 `ROOT_APPROVED`와 릴리스 `ROOT_APPROVED_RELEASED`를 각 자산의 원본·파생 해시 사슬로 24/24 검증했다. 대체 선택 영수증은 소스·복사본 SHA, 권한 파일 해시, 오너의 플랫폼 간 재사용 승인과 기존 원본 보존 사실을 결속하며 상태는 `READY_FOR_ROOT_VISUAL_REVIEW`다.
- 24장을 접촉시트로 직접 확인해 성인 한국 여성 1명, 휴대폰을 든 실제 거울 셀피, 완전 착의, 일관된 반사, 문자·로고·워터마크·기형·중복 인물 없음 조건에서 24개 ACCEPT, 0개 REJECT로 판정했다. 이미지 생성 호출과 기존 campaign/reuse manifest·focal JSON·공용 스크립트·앱·테스트·릴리스 변경은 0건이다.

## 2026-08-18 KST — 지역 콘텐츠·메타·RSS 정본화

- 활성 1,291개 경로를 `root || displayName /시$/` 기준의 광역 41개와 세부 1,250개로 고정했다. 광역은 11개 섹션×문단 2개, 세부는 10개 섹션×문단 2개이며 두 유형 모두 주소 디렉터리를 마지막 콘텐츠 섹션에 둔다.
- 지역 본문 문단 25,902개를 마사지데이 전용 사실 문장으로 작성했다. raw 문단과 정규화 전체 페이지 signature는 모두 고유하며 정규화 문단 슬롯의 최대 재사용은 광역 5회, 세부 4회다.
- 지역 meta title·description·H1을 서로소 순환 문형과 실제 부모·형제·하위·별칭 주소 사실로 분산했다. 정규화 결과 title 17형, H1 19형, description 1,291형이며 description 최대 재사용은 1회다.
- preview `.invalid` self canonical과 `noindex,nofollow,nocache`를 유지했다. 지역 JSON-LD는 화면 본문과 breadcrumb에서 만든 `WebPage`·`BreadcrumbList`만 사용하고 직렬화 단계에서 `<`를 이스케이프한다.
- 마사지데이 전용 블로그 글 2개와 실제 게시·수정일, 영구 URL GUID, 전체 HTML 본문을 포함한 RSS 2.0을 구성했다. 지역 URL 1,291개는 RSS가 아니라 1,299개 URL sitemap에서 관리한다.
- 지정된 기존 플랫폼 정본 7개를 빠짐없이 확인하는 copy audit를 hard-fail 방식으로 바꾸고, 장문 exact 및 브랜드·지역 정규화 충돌 0건을 확인했다.
- 콘텐츠 집중 테스트 27개, copy audit, 전체 TypeScript 검사, 소유 파일 ESLint를 통과했다. 이번 작업에서는 build·commit·push를 실행하지 않았다.

## 2026-08-18 KST — Template6 이미지 캠페인·브랜드 파이프라인

- 지역 배너 1,291개 경로를 216개 마스터에 결정적으로 배정했다. 초기 계획의 재사용 72개는 랑테라피 승인 릴리스 24개, 건마에반하다 루트 승인본 24개, 혼혈마사지 `replacement-new` 승인본 24개였다. 이후 실제 시각 검수에서 랑테라피 24개를 활성 선택에서 제외하고 필링홈타이 거울 셀피 24개로 대체했으며, 최종 구성은 위 릴리스 항목을 정본으로 삼는다.
- 재사용 마스터는 430개 경로(71개×6회, 1개×4회), 신규 마스터는 861개 경로(143개×6회, 1개×3회)에 배정했다. 광역 상세 41개는 모두 신규이며 부모·자식 및 형제 이미지 충돌은 각각 0건이다.
- 신규 지역 이미지 `mday-t6-rgn-073-v1`~`216-v1` 144개와 홈 히어로·블로그 배너 3개의 고정 프롬프트·단일 호출 작업 파일을 만들었다. 이 스캐폴드 작성 과정의 이미지 생성 호출은 0회다.
- 루트 검수가 끝나기 전에는 활성 파생물을 만들 수 없도록 생성 영수증, 접촉시트, 해시 결속 루트 승인, 반응형 파생·릴리스 순서의 실패 차단 게이트를 추가했다. `073`~`078`은 별도 루트 작업에서 각각 한 번 생성된 원본을 확인하고 생성 영수증 6개를 기록했으며, 아직 `ROOT_APPROVED_RELEASED` 상태가 아니다.
- 브랜드 마크는 기존 하트·궤도와 겹치지 않는 낮의 해와 수평선 모티프의 투명 SVG로 작성하고 32·192·512px PNG를 파생했다. 이미지 생성 호출은 사용하지 않았고 SVG 및 PNG 해시는 provenance에 결속했다.
- 이미지 전용 테스트 7개와 관련 ESLint 검사를 통과했다. 전체 TypeScript 검사는 이미지 코드 밖의 `tests/rss-feed.test.ts` 날짜 reduce 타입 오류로 중단됐으며, 이미지 전용 테스트·린트에는 오류가 없다.

## 2026-08-18 KST — Template6 신규 플랫폼 착수

- 브랜드 `마사지데이`, 플랫폼 ID `massage-day`, 시각 정본 Template6를 확정했다.
- 마사지봄과 동일한 활성 1,291개 지역 그래프를 사용하고 광역 상세 41개, 세부 상세 1,250개로 나눈다. 고객 문구와 메타 3종은 기존 플랫폼 문장을 복사하지 않고 마사지데이 전용으로 다시 작성한다.
- 도메인이 정해지기 전에는 preview `.invalid`, noindex/nofollow/nocache, robots 전체 차단을 유지한다. sitemap, RSS, GA4 코드는 준비하되 다른 플랫폼 도메인·소유확인 값·측정 ID는 가져오지 않는다.
- 지역 배너는 기존 승인본 72개와 신규 144개, 총 216개로 구성한다. 모든 원본을 최대 6개 경로에만 배정하고 부모·자식·형제 충돌을 막는다.
- Template6에는 코스 이미지 슬롯이 없으므로 거울 셀피는 히어로·지역·블로그 배너와 에디토리얼 카드에만 사용하고 코스·가격은 텍스트 카드로 유지한다.
